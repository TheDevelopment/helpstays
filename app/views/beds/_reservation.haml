%table.table{:cellpadding => 0, :cellspacing => 0}
  %th Organisation
  %th Start Date 
  %th End Date

  %tr{:class => cycle("even","odd")}
    %td= reservation.organisation.name
    %td= reservation.start_date.to_formatted_s(:long) 
    %td= reservation.end_date.to_formatted_s(:long) 
