require 'spec_helper'

describe Roles do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Roles.create!(@valid_attributes)
  end
end
