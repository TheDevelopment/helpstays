require 'spec_helper'

describe OrganisationType do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    OrganisationType.create!(@valid_attributes)
  end
end
